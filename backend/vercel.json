{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "echo 'Backend - no build'",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  }
}
